services:
  klinok-ui:
    image: klinok.ui:local
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      API_URL: ${API_URL:-http://localhost:8080/api}
      ENABLE_LOG: ${ENABLE_LOG:-false}
